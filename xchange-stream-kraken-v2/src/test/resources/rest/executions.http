### Get Websockets Token
< {%
  import {gen_sign} from 'sign.js'
  gen_sign(request);
%}
POST {{rest_base_url}}/private/GetWebSocketsToken
API-Key: {{api_key}}
API-Sign: {{sign}}
Content-Type: application/json

{
  "nonce": {{nonceValue}}
}

> {% client.global.set("websocket-token", response.body.result.token); %}


### Subscribe User Trades Channel
WEBSOCKET {{base_auth_url}}
Content-Type: application/json

{
  "method": "subscribe",
  "params": {
    "channel": "executions",
    "snap_trades": false,
    "snap_orders": false,
    "token": "{{websocket-token}}"
  }
}

### Unsubscribe User Trades Channel
WEBSOCKET {{base_url}}
Content-Type: application/json

{
  "method": "unsubscribe",
  "params": {
    "channel": "executions",
    "token": "{{websocket-token}}"
  }
}

